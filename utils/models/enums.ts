export enum MortgageState {
    Nil,
    Pending,
    Supplied,
    Repaid,
    Foreclosed,
    Cancelled
}

export enum OfferState {
    Nil,
    Selling,
    Sold,
    Cancelled
}

export enum StakeTokenOperation {
    Stake,
    Unstake,
    FetchReward,
    Promote,
    Transfer
}
