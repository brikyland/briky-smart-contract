# Solidity API

## ProjectTokenConstant

@author Briky Team

 @notice Constant library for contract `ProjectToken`.

### TOKEN_DECIMALS

```solidity
uint8 TOKEN_DECIMALS
```

Each unit of project token is represented in scaled form as `10 ** TOKEN_DECIMALS`.

