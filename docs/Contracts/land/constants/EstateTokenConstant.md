# EstateTokenConstant

Constant library for contract `EstateToken`.

## TOKEN_DECIMALS

```solidity
uint8 TOKEN_DECIMALS
```

Each unit of estate token is represented in scaled form as `10 ** TOKEN_DECIMALS`.

